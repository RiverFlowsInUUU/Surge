# 08 · 审计读数

> 本仓库自带审计脚本，可直接对模板运行。脚本在 [`skill/scripts/`](../skill/scripts/)，
> 命令清单与逐条判据见 [`skill/reference/checker.md`](../skill/reference/checker.md)。
>
> ⚠️ **全部验证都在本地完成** —— 本仓库**刻意不挂 CI / 任何自动化**（理由见 [`skill/README.md`](../skill/README.md)）。

---

## 对推荐版 `v1` 的读数

| 脚本 | 读数 |
|:----:|:----:|
| `check_surge_dns.py` | ✅ **0 high**（退出码 0），另有 0 medium / 2 low / 12 ok / 2 waived |
| `audit_ruleset_content.py` | ✅ 全部通过 —— 7 条 `RULE-SET` 里 5 条远程（2 条内置），**IP 类条目缺 no-resolve = 0** |
| `audit_routing_coverage.py` | ✅ 33/33 —— 国内探针 17/17 命中 `DIRECT` · 境外探针 8/8 走代理 · 误杀探针 8/8 未被拦 |
| `skill/tests/architecture.sh` | ✅ 9/9 —— 占位符纪律 · DNS 段一致性 · 规则顺序铁律 |

`v0` 的读数与 `v1` 一致（`check_surge_dns` 同样 0 high / 2 low / 12 ok / 2 waived），
差异只在规则数与分流覆盖面。

---

## 那 2 条 `WAIVED` 是什么

**不是缺陷，是刻意的设计取舍**（豁免理由写在 profile 文件里，可 grep `audit-waive`）：

1. **加密 DNS 端点保留 2 个主机名形式**（`dns.google` / `dns.alidns.com`）——
   这两个名字会被引导解析一次。换成纯 IP 字面量能消掉它，但会失去
   「按域名走 CDN 就近解析」与「ECS 合规」两项收益。
2. **兜底指 `Proxy` 而非国内组** —— 本模板不做分流兜底的境内 / 境外切分。

---

## 那 2 条 `LOW` 是什么

同样不是缺陷，且**判据本身就不该判负**：

1. **`hijack-dns` 未覆盖全部知名境外解析器** —— `:53` 的地址空间是无限的，
   列举永远不可能"列全"。本模板覆盖了 6 个最常见的（Google / Cloudflare /
   Quad9 / OpenDNS），够用；想一网打尽就写 `hijack-dns = *`。
2. **部分 `always-real-ip` 主机名未被本文件的域名规则接住** ——
   它们可能在远程规则集里被接住，而**远程规则集的内容无法静态判定**。
   真实答案由 `audit_routing_coverage.py` 拿域名走一遍给出（当前 8/8 未被误杀）。

---

## 回归测试

```bash
bash skill/tests/run.sh                   # 默认跑全部 5 个阶段
SKIP_NET=1 bash skill/tests/run.sh        # 跳过需要联网的阶段 4
PY=/path/to/python bash skill/tests/run.sh
```

五个阶段共 **13 个断言**，退出码非 0 即失败：

| 阶段 | 断言对象 | 断言数 |
|:----:|:---------|:------:|
| 1 | 3 个 fixture × `check_surge_dns.py` | 3 |
| 2 | 4 份 `profiles/*.conf` × `check_surge_dns.py` | 4 |
| 3 | `architecture.sh`（架构不变量） | 1 |
| 4 | 2 份 profile × 2 个联网审计脚本 | 4 |
| 5 | `check_links.py`（markdown 链接与锚点） | 1 |

> ⚠️ **阶段 1 里有两个"期望判负"的 fixture**（`bad_bootstrap` / `bad_order_and_policy`）。
> 它们的存在是为了证明审计器**真的有判别力**，而不是恒返回 0。
> 也因此，`run.sh` 一开始就有「解释器与依赖」的前置检查：解释器坏掉时脚本会以
> **退出码 1** 结束，而那正是 `bad_*` 期望的值 —— 会被误判成"通过"。
> 环境故障用退出码 **2** 单独表示。
