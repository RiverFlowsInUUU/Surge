# 更新日志

本文件按 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范编写。

---

## 2026-09-21

### 新增

- 📄 **两版配置**：`v1` 完整分流版（3 组 / 12 条规则）、`v0` 极简版（2 组 / 7 条规则），
  各有带注释与纯配置两种形态；两版的防 DNS 泄露结构（DNS / IPv6 / 测试端点）完全一致
- 🧪 **三个审计脚本**：防泄露结构审计（12 项）、远程规则集内容审计、分流覆盖审计；
  都是仅依赖 Python 标准库的单文件脚本
- ✅ **回归测试**：5 个阶段、13 个断言，含两个「期望判负」的坏配置样本，
  用于证明审计脚本真的有判别能力
- 📐 **架构不变量检查**：占位符纪律、两版 DNS 段一致性、规则顺序铁律
- 📚 **专题文档 10 篇**：DNS 怎么工作 / 为什么泄露 / 12 项加固清单 / 逐段讲解 /
  分流与 `no-resolve` 必须成对交付 / 实测数据 / 文件版本沿革 / 审计读数 /
  注意事项 / 图标与许可
- 📘 **完整技术文档**：18 节，含原理推导、逐键理由、已知取舍与 FAQ
- 🧩 **方法论包**：审计流程、加固模板、坑复盘、泄露定位、规则集轻重、公开仓库维护

### 变更

- 🔄 **测速端点改用国内 204**：`proxy-test-url` 由境外地址改为
  `connect.rom.miui.com/generate_204` —— 该端点每个策略组每 5 分钟被访问一次，
  使用境外地址会形成持续存在的境外域名解析
- 🔒 **所有 IP 类规则加 `no-resolve`**：消除「规则触发解析」这条明文通路
- 🎛️ **`Proxy` / `AI` 组改用 `smart`**：按真实首字节延迟、TCP 重传、UDP 响应评分
- 🤖 **AI 流量独立成组**：出口与日常流量分开
- 🚫 **广告拦截启用 `pre-matching` + `extended-matching`**：拦截在
  DNS 查询与连接建立阶段生效

### 修复

- 🐛 **补上国内直连的域名类规则集**：给 IP 规则加 `no-resolve` 会同时关闭
  「解析后判 IP 归属」这条直连通路，若不另配域名类规则集，国内网站会整片走代理
- 🐛 **修正配置内关于广告组的说明**：广告拦截使用字面量策略，
  手动切换广告组不会改变拦截行为
- 🐛 **移除空转参数**：`update-interval` 写在非订阅型策略组上不生效

### 说明

- 🔐 **本仓库是脱敏模板**。所有节点地址均为文档专用地址段，凭据均为占位符，
  导入前需替换为自己的节点
- 🧪 **全部验证都在本地完成**，本仓库不挂 CI / 任何自动化
- ⚠️ 审计脚本只覆盖可静态判定的部分。拦截效果、误杀与节点可用性需自行实测
